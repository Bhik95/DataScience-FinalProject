fruits_train -> the jupyter notebook used to train the CNNs without pre-training (type A)
fruits_train_pre_trained -> the jupyter notebook used to train the CNNs starting from MobileNet V2 (type B)
fruits_train_baseline -> the jupyter notebook used to train the logistic regression model (C1)